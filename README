
创建本地分支
git branch local_branch
	git add ..
	git commint ...

提交新的分支到服务器
git push origin local_branch:origin_branch


git push origin local_branch 这样会直接把本地local_branch 分支上传的远程的同名的分支上，如果没此分支，就会新建

git checkout -b new_branch  新建一个分支并跳转
git branch new_branch 新建一个分支，不跳转

git push  origin --delete origin_branch 删除远程分支 ，注意：不是git push origin --delete origin/origin_branch
